% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xmap_diagnosis.R
\name{new_xmap_diagnosis}
\alias{new_xmap_diagnosis}
\title{Construct an \code{xmap_diagnosis} object}
\usage{
new_xmap_diagnosis(valid, details, labels, class = character())
}
\arguments{
\item{valid}{A single logical: did every check pass?}

\item{details}{A named list of tibbles (or \code{NULL}), one entry per check.
\code{NULL} means that check passed; a tibble holds the offending locations.}

\item{labels}{A named list, one entry per check (same names as
\code{details}), each entry a length-2 character vector with elements
\code{pass} and \code{fail} — the text to show when that check passed or
failed, respectively. A single check reads differently depending on
outcome (e.g. "No duplicate pairs" vs. "Duplicate pairs found").}

\item{class}{Additional subclass(es) to prepend, e.g. \code{"xmap_diagnosis_tbl"},
for representation-specific methods beyond printing.}
}
\value{
An \code{xmap_diagnosis} object.
}
\description{
\code{xmap_diagnosis} is the shared return contract for \verb{diagnose_as_xmap_*()}
functions across crossmap representations (data frame, matrix, graph,
...): a single object shape regardless of whether the diagnosis passed
or failed, so callers can inspect \verb{$valid} programmatically, or print
the object for a human-readable report of what needs fixing.
}
\details{
The set of checks, their labels, and what counts as an "offending
location" (rows, matrix cells, graph edges, ...) is specific to each
representation. Callers of \code{new_xmap_diagnosis()} supply \code{labels} for
their own checks, and are responsible for normalizing each check's
offending locations into a tibble before passing them in \code{details} —
\code{print.xmap_diagnosis()} stays representation-agnostic by only ever
printing tibbles.
}
\keyword{internal}
