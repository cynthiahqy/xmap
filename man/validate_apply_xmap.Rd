% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_xmap.r
\name{validate_apply_xmap}
\alias{validate_apply_xmap}
\title{Cheaply check whether \code{.data} is conformable with an \code{xmap_tbl}}
\usage{
validate_apply_xmap(
  .data,
  .xmap,
  values_from,
  keys_from = names(.xmap$.from),
  ...
)
}
\arguments{
\item{.data}{The dataset to transform.}

\item{.xmap}{An \code{xmap_tbl} object.}

\item{values_from}{A \code{tidyselect} expression of columns in \code{.data}
with values to transform}

\item{keys_from}{A \code{tidyselect} expression specifies the column in \code{.data}
to match with \code{.xmap$from}}

\item{...}{(reserved)}
}
\value{
A single logical.
}
\description{
\code{validate_apply_xmap()} checks the same two conditions \code{apply_xmap()}
requires before transforming \code{.data} -- every \code{keys_from} key has a
matching \code{.xmap$.from} link, and no \code{values_from} column has a missing
value -- and returns a single logical, without building the
offending-rows/columns detail objects that \code{diagnose_apply_xmap()} does.
It's the primitive to reach for when you only need a pass/fail answer --
e.g. checking many \code{.data}/\code{.xmap} group pairs with \code{dplyr::mutate()} or
\code{purrr::map2_lgl()} before applying any of them. Reach for
\code{diagnose_apply_xmap()} once \code{validate_apply_xmap()} says something
failed and you need to know why; \code{apply_xmap()} checks the same
conditions at transform time and aborts with a message pointing at the
offending condition.
}
\examples{
abc_xmap <- demo$abc_links |>
  as_xmap_tbl(from = "lower", to = "upper", weight_by = "share")
abc_data <- tibble::tibble(
  lower = unique(demo$abc_links$lower),
  count = runif(length(unique(demo$abc_links$lower)), min = 100, max = 500)
)
validate_apply_xmap(abc_data, abc_xmap, values_from = count)
}
