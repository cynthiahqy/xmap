% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compose_xmap.R
\name{compose_xmap}
\alias{compose_xmap}
\title{Compose Two Crossmaps Through a Shared Intermediate Classification}
\usage{
compose_xmap(xmap1, xmap2, ..., tol = .Machine$double.eps^0.5)
}
\arguments{
\item{xmap1}{An \code{xmap_tbl}, \code{S -> M}.}

\item{xmap2}{An \code{xmap_tbl}, \code{M -> T}. Every value in \code{xmap1}'s \code{.to}
must appear in \code{xmap2}'s \code{.from}; the reverse isn't required -- \code{xmap2}
may hold \code{.from} values \code{xmap1} never uses.}

\item{...}{(reserved)}

\item{tol}{Tolerance of comparison.}
}
\value{
An \code{xmap_tbl}, \code{S -> T}.
}
\description{
Given \code{xmap1} (\code{S -> M}) and \code{xmap2} (\code{M -> T}) sharing intermediate
key set \code{M}, chains them into a single crossmap \code{S -> T} without
materialising \code{M}-level values. Composed weights sum, over every
shared \code{m}, the product of \code{xmap1}'s weight onto \code{m} and \code{xmap2}'s
weight from \code{m}:
\deqn{w(s, t) = \sum_{m \in M} w_1(s, m) \, w_2(m, t)}{
  w(s, t) = sum over m in M of  w1(s, m) * w2(m, t)}
}
\details{
Re-checks that both inputs are actually valid crossmaps, not just
correctly classed, and aborts otherwise.

Only takes two crossmaps at a time. Matrix multiplication is
associative, so chain longer sequences with \code{Reduce()} instead of a
dedicated variadic interface -- see the example below. Grouped
composition (e.g. one \code{xmap1} per group, composed against a shared
\code{xmap2}) is likewise left to the caller via \code{dplyr::group_map()}.

\strong{Known limitation:} composing two individually-\code{tol}-valid crossmaps
can produce a composed crossmap that fails that same \code{tol}. Composed
weights are sums of products of the input weights, which amplifies
floating-point drift relative to either input alone -- and compounds
further across a \code{Reduce()}-chained sequence. Widening \code{tol} on the
\code{compose_xmap()} call (or on the final \code{Reduce()} step) works around
this in practice, but the underlying cause is \code{.weight_by} being plain
\code{double} rather than a representation with an exact sum-to-1 guarantee
(see #27).
}
\examples{
abc_xmap <- demo$abc_links |>
  as_xmap_tbl(from = lower, to = upper, weight_by = share)
top_xmap <- tibble::tibble(
  upper = c("AA", "BB", "CC", "DD", "EE"),
  top = c("AAA", "AAA", "BBB", "BBB", "BBB"),
  weight = 1
) |>
  as_xmap_tbl(from = upper, to = top, weight_by = weight)
compose_xmap(abc_xmap, top_xmap)

# chaining more than two crossmaps: reduce pairwise composition over a
# list, e.g. lower -> upper -> top -> region
region_xmap <- tibble::tibble(
  top = c("AAA", "BBB"),
  region = c("north", "south"),
  weight = 1
) |>
  as_xmap_tbl(from = top, to = region, weight_by = weight)
Reduce(compose_xmap, list(abc_xmap, top_xmap, region_xmap))
}
