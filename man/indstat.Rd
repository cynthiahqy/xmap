% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{indstat}
\alias{indstat}
\title{UNIDO INDSTAT4 industrial statistics (masked), with country lookup}
\format{
\subsection{\code{indstat}}{

A list with:
\describe{
\item{masked_sample}{tibble with 17,365 rows and 11 columns:
\describe{
\item{ctable}{table code; \code{14} (the only value in this subset)
denotes the OUTPUT dimension of INDSTAT4}
\item{country}{three-digit UN M49 country code (joins onto
\code{country_lookup$code}); 133 distinct countries in the full
INDSTAT4 Rev.3 dataset, 8 in this subset}
\item{year}{observation year (1990-2013)}
\item{isic}{3- or 4-digit ISIC Rev.3 industry code; every 4-digit
code nests inside the 3-digit code given by its first three
digits}
\item{isiccomb}{ISIC code as originally reported -- either the
same as \code{isic}, or a combined code (containing a letter, e.g.
\code{"151A"}) covering several \code{isic} codes at once}
\item{value}{reported output value in USD, masked to \code{1000} in
this dataset (real values are not shipped); \code{NA} for \code{isic}
codes with no directly reported value (i.e. covered only by
another row's \code{isiccomb})}
\item{utable}{output valuation methodology, consistent within a
country/year but variable across countries: \code{11} = basic prices,
\code{12} = factor prices, \code{13} = producers' prices, \code{14} = valuation
not defined}
\item{source}{reporting-status flag (\code{0}-\code{3}) -- exact code meanings are
undocumented upstream, not just unconfirmed here (see reference
below)}
\item{unit}{value unit; always \code{"$"} (USD) in INDSTAT4, no
national-currency variants}
\item{country_iso3c}{ISO-3c country code, joined from
\code{country_lookup}}
\item{country_name}{country name, joined from \code{country_lookup}}
}}
\item{country_lookup}{tibble with 8 rows and 4 columns, a small
lookup table of the 8 countries included in \code{masked_sample}, used to
join ISO-3c codes and country names onto it:
\describe{
\item{code}{three-digit UN M49 country code, joins onto
\code{masked_sample$country}}
\item{name}{country name}
\item{iso3c}{ISO-3c country code}
\item{income_group}{World Bank income group classification, 2006
vintage: \code{"H"} = high income, \code{"UM"} = upper-middle income,
\code{"LM"} = lower-middle income, \code{"L"} = low income. All four groups
are represented in this subset}
}}
\item{isic_rev3_lookup}{tibble with 529 rows and 4 columns, the full
ISIC Rev. 3 classification hierarchy, giving a label for every
\code{isic}/\code{isiccomb} code number used in \code{masked_sample}:
\describe{
\item{code}{ISIC Rev. 3 code: a single letter for \code{"section"}
(17 rows), otherwise 2/3/4 digits for \code{"division"}/\code{"group"}/\code{"class"}}
\item{level}{one of \code{"section"}, \code{"division"}, \code{"group"}, \code{"class"},
determined by the length of \code{code}}
\item{label}{English description of the code}
\item{parent_code}{code of the immediate numeric parent -- a
\code{"class"} code's first three digits (its \code{"group"}), or a
\code{"group"} code's first two digits (its \code{"division"}). \code{NA} for
\code{"section"} and \code{"division"}, since sections cover ranges of
divisions rather than sharing a numeric prefix with them}
}}
}
}
}
\source{
\code{masked_sample}: downloaded and parsed from the UNIDO
INDSTAT4 website (Rev.3, 2019 vintage). See
\url{https://cynthiahqy.github.io/indstat-TPP/001-clean_INDSTAT.html}
for the cleaning pipeline this subset was derived from.

\code{country_lookup}: read by \code{data-raw/indstat.R} from
\code{data-raw/indstat-country-lookup.csv}, exported alongside
\code{masked_sample} by the same upstream script. \code{income_group} is the
\code{2006} column of the World Bank's historical income classification
workbook ("Country Analytical History" sheet of \code{OGHIST.xlsx});
current download at
\url{https://datahelpdesk.worldbank.org/knowledgebase/articles/906519-world-bank-country-and-lending-groups}

\code{isic_rev3_lookup}: UN Statistics Division classifications registry,
ISIC Rev. 3 English structure file, downloaded from
\url{https://unstats.un.org/unsd/classifications/Econ/Download/In\%20Text/ISIC_Rev_3_english_structure.Txt}
into \code{data-raw/isic_rev3_structure.txt}. See #34.
}
\usage{
indstat
}
\description{
A subset of UNIDO's INDSTAT4 industrial-statistics database, with the
reported output \code{value} masked to a constant, bundled together with a
small country-code lookup table since the two are relationally paired
(\code{indstat$masked_sample$country} joins onto
\code{indstat$country_lookup$code}). Used in
\code{vignette("extract-validate-existing")} (Case 2) to
demonstrate grouped crossmap validation across \code{country}/\code{year}. Some
\code{isic} industry codes are reported only in combination (\code{isiccomb}),
with a single \code{value} covering several \code{isic} codes at once -- the
vignette splits these back out.
}
\details{
The 8 reporters are five large economies (BRA, CHN, DEU, JPN, USA)
plus three chosen for structurally distinct splitting behaviour once
the split is re-aggregated to 3-digit ISIC: Colombia (splits are
entirely reconvergent -- imputed at 4 digits, exact at 3), Romania
(the deepest sustained convergence in the source extract) and Yemen
(~95\% of \code{isic} values sit in splits that cross a 3-digit boundary).
}
\keyword{datasets}
