% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_xmap.r
\name{check_conformable_xmap_data}
\alias{check_conformable_xmap_data}
\title{Check whether matched keys/values are conformable with an \code{xmap_tbl}'s
\code{.from} set (internal)}
\usage{
check_conformable_xmap_data(key, value, from)
}
\arguments{
\item{key}{A vector of matched keys from \code{.data} (\code{key_val$.key}).}

\item{value}{A data frame of matched value column(s) from \code{.data}
(\code{kv_tbl$.value}); checked column-by-column since more than one
\code{values_from} column may be selected.}

\item{from}{\code{.xmap$.from} to check \code{key}'s coverage against.}
}
\value{
A list of two logicals: \code{covered} (every \code{key} has a matching
\code{from}) and \code{no_missing_values} (no \code{value} column has a missing value).
}
\description{
The single source of truth for the two data-conformability conditions
\code{apply_xmap()} requires before transforming, shared by \code{apply_xmap()}'s
abort gate, \code{diagnose_apply_xmap()}'s flags, and \code{validate_apply_xmap()},
so the three don't independently re-implement (and risk drifting on) the
same checks.
}
\keyword{internal}
