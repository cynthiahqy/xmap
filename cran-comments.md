## Submission

This is an update to `xmap`, a package previously accepted on CRAN (0.1.0, accepted 2025-01-31).

## R CMD check results

0 errors | 0 warnings | 0 notes

Checked locally on a clean checkout (macOS, R 4.5.2, `devtools::check(cran = TRUE)`).

## win-builder

`devtools::check_win_devel()` results pending -- to be added once the win-builder run completes.

## Downstream dependencies

There are currently no reverse dependencies for `xmap` on CRAN.
